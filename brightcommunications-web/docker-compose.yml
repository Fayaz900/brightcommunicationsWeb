services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: bright
      POSTGRES_PASSWORD: bright
      POSTGRES_DB: brightcommunications
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
